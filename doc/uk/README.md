# ioBroker.miboxer-wl433

> [English README](../../README.md)

---

<p align="center">
  <a href="https://www.buymeacoffee.com/ssbingo"><img alt="Buy me a coffee" src="https://img.buymeacoffee.com/button-api/?text=Buy%20me%20a%20coffee&emoji=&slug=ssbingo&button_colour=FFDD00&font_colour=000000&font_family=Cookie&outline_colour=000000&coffee_colour=ffffff" /></a>
</p>

---

Локальне керування світильниками для басейну **MiBoxer PW01 / PW02** (LoRa, 433 МГц) через шлюз **MiBoxer WL-433** — без хмари, без голосового асистента, безпосередньо у вашій локальній мережі.

Виробник: [MiBoxer (Futlight Optoelectronics)](https://miboxer.com/) — [WL-433](https://miboxer.com/product/lora-433mhz-gateway), [PW01](https://miboxer.com/product/27w-rgbcct-par56-led-pool-light-lora-433mhz)

## Відмова від відповідальності

Це **неофіційний проєкт спільноти**. Він **не пов'язаний** із Shenzhen Futlight Optoelectronics Co., Ltd. (MiBoxer / Mi-Light) або Tuya і не підтримується ними. «MiBoxer», «Mi-Light» і «Tuya» є торговими марками відповідних власників і використовуються лише для опису сумісності пристроїв. Використовуйте на власний ризик.

## Принцип роботи

Усередині WL-433 є модуль Wi-Fi Tuya. У локальній мережі шлюз є **одним** пристроєм Tuya: усі прив'язані лампи керуються через цей пристрій, а шлюз передає команди лампам через LoRa (433 МГц). Адаптер напряму спілкується зі шлюзом за **протоколом Tuya LAN 3.3** (TCP-порт 6668, шифрування AES локальним ключем) на основі перевіреної бібліотеки [tuyapi](https://github.com/codetheweb/tuyapi) (її також використовує ioBroker.tuya). Підтримуються й версії протоколу 3.1, 3.4 та 3.5 на випадок, якщо оновлення прошивки її змінить.

Лампи, зони та сцени керуються власними командами шлюзу в специфічній для виробника **точці даних 101** — тими самими командами, що надсилає застосунок MiBoxer. Шлюз повідомляє свій стан так само; крім того, адаптер запитує його під час підключення та під час кожного оновлення стану.

```text
ioBroker ──LAN: Tuya 3.3, TCP 6668──► WL-433 ──LoRa 433 MHz──► PW01 / PW02
```

**Посібник користувача**, у якому кожен крок пояснено для початківців (встановлення, налаштування, зони, таймери, приклади, усунення несправностей): [English](../Manual_miboxer-wl433.md) ([PDF](../Manual_miboxer-wl433.pdf)) · [Deutsch](../Handbuch_miboxer-wl433.md) ([PDF](../Handbuch_miboxer-wl433.pdf)).

Вихідне дослідження (аналіз протоколу, джерела, план перевірки, розшифрована точка даних 101), німецькою: [Miboxer_WL-433_PW01_Protokollanalyse_lokale_Steuerung.md](../Miboxer_WL-433_PW01_Protokollanalyse_lokale_Steuerung.md) ([PDF](../Miboxer_WL-433_PW01_Protokollanalyse_lokale_Steuerung.pdf)). Інструкція з прив'язування ламп до шлюзу: [Anleitung_PW01_mit_WL-433_verbinden.pdf](../Anleitung_PW01_mit_WL-433_verbinden.pdf).

## Підтримуване обладнання

| Пристрій | Роль | Статус |
| --- | --- | --- |
| MiBoxer WL-433 | Обов'язковий, адаптер підключається до нього | Перевірено з реальним шлюзом (протокол Tuya 3.3, точка даних 101) |
| MiBoxer PW01 (27 W RGB+CCT PAR56) | Лампа, прив'язана до шлюзу | Цільовий пристрій |
| MiBoxer PW02 (18 W RGB+CCT PAR56) | Лампа, прив'язана до шлюзу | Та сама родина продуктів, має працювати |
| MiBoxer UW01, UW02, UW03, RD-9L | Лампа, прив'язана до шлюзу | Не перевірено |

## Вимоги

1. Шлюз налаштовано в застосунку MiBoxer, і лампи до нього прив'язано.
2. **ID пристрою та локальний ключ** шлюзу. Виробник не підтримує платформу розробників Tuya для WL-433, але застосунок MiBoxer записує обидва значення у свій журнал налагодження: на Android читайте журнал переглядачем logcat, наприклад *LogFox*, поки застосунок запускається та керує шлюзом. **Локальний ключ змінюється під час кожного повторного сполучення шлюзу** — тоді його треба знову прочитати й ввести. Покрокова інструкція для початківців (німецькою, для Android, iPhone та iPad): [Anleitung_Geraete-ID_und_Local-Key_auslesen.md](../Anleitung_Geraete-ID_und_Local-Key_auslesen.md) ([PDF](../Anleitung_Geraete-ID_und_Local-Key_auslesen.pdf)).
3. Шлюз доступний з ioBroker (та сама мережа). Рекомендовано резервування DHCP; без указаної IP-адреси адаптер знаходить шлюз за його широкомовними UDP-повідомленнями (порти 6666/6667).
4. **Пристрої Tuya зазвичай приймають лише одне локальне підключення.** У тесті застосунок MiBoxer і адаптер були підключені одночасно; але якщо підключення знову й знову не вдається, закрийте застосунок на телефонах у тій самій мережі й не керуйте шлюзом одночасно іншими локальними інтеграціями (ioBroker.tuya, Home Assistant, tinytuya).

## Налаштування

Налаштування екземпляра мають дві вкладки: **Шлюз** (підключення та керування зонами) і **Таймери** (див. [Таймери](#таймери)).

| Параметр | Опис |
| --- | --- |
| ID пристрою | ID пристрою Tuya шлюзу WL-433 |
| Локальний ключ | 16-символьний локальний ключ Tuya (зберігається зашифрованим) |
| IP-адреса шлюзу | Залиште порожнім, щоб знайти шлюз у локальній мережі автоматично |
| Версія протоколу Tuya | 3.3 для WL-433 (можна вибрати 3.1, 3.4 та 3.5) |
| Знайти шлюз у локальній мережі | Кнопка: знаходить шлюз за ID пристрою та вписує IP-адресу й версію протоколу. Без ID пристрою виводиться список усіх знайдених пристроїв Tuya |
| Затримка перепідключення | Секунди до повторної спроби втраченого або невдалого підключення (типово 30) |
| Інтервал оновлення стану | Секунди між повними запитами стану (типово 60, 0 = лише оновлення, які надсилає шлюз) |
| Керування зонами | *Вибір зони* (типово) або *окремий канал для кожної зони*, див. [Зони](#зони) |

## Зони

Шлюз керує до 8 зонами (як пульт FUT086). Кожна команда може йти до однієї зони або до всіх зон, але шлюз повідомляє **лише один стан для всіх ламп: останнє налаштування, незалежно від того, до якої зони його надіслано**. Застосунок MiBoxer теж не показує окремий стан для кожної зони. Параметр *Керування зонами* пропонує два варіанти:

| Варіант | Стани | Підходить для |
| --- | --- | --- |
| **Вибір зони (типово)** | `light.*` показує стан шлюзу. `light.zone` (0 = усі зони, 1–8) визначає, до якої зони надсилаються команди `light.*`. | Більшість користувачів: кожен стан показує те, що повідомляє шлюз |
| **Окремий канал для кожної зони** | `light.*` показує стан шлюзу й надсилає команди до всіх зон. Крім того, `zones.zone1` … `zones.zone8` керують кожною зоною окремо. Канал зони показує останні значення, надіслані до цієї зони та підтверджені шлюзом; він залишається порожнім, доки до зони нічого не надіслано. | Скрипти та візуалізації, які звертаються до зон напряму |

Після зміни параметра стани іншого варіанта видаляються.

## Таймери

Вкладка **Таймери** в налаштуваннях екземпляра містить до **50 таймерів**. Вони працюють локально в адаптері, зокрема без інтернету, і вміють більше, ніж таймери застосунку MiBoxer: сонячні події зі зсувом, випадкове відхилення, сезон, зони, кольори, сцени та вимкнення через заданий час. Додайте таймер кнопкою **+**, відкрийте його, щоб змінити, копіюйте або видаляйте його кнопками запису. Зміни набувають чинності після збереження налаштувань (екземпляр перезапускається).

| Поле | Опис |
| --- | --- |
| Активний | Вимикає цей таймер, не видаляючи його |
| Назва | Показується в журналі та в `timers.overview` |
| Тригер | *Час доби* або сонячна подія: світанок, схід сонця, золота година (вечір), захід сонця, сутінки, ніч |
| Час доби | Лише для тригера *Час доби* |
| Зсув | Хвилини (від −720 до 720), від'ємне значення = раніше — наприклад, захід сонця −15 |
| Випадкове відхилення | До ± хвилин (0–120), випадково обирається заново для кожного запуску — для імітації присутності |
| Дні тижня | Дні, у які спрацьовує таймер |
| Сезон з / по | `DD.MM.`, наприклад з `01.05.` по `30.09.`; сезон, що переходить через Новий рік (з `01.11.` по `28.02.`), теж можливий; порожньо = весь рік |
| Зона | Усі зони або зона 1–8 |
| Дія | Увімкнути, вимкнути, біле світло (колірна температура 2700–6500 K), колір, сцена M1–M9, лише яскравість |
| Яскравість | 1–100 %, порожньо = без змін (крім дії *Вимкнути*) |
| Вимкнути через | Хвилини (0–1440), 0 = не вимикати (крім дії *Вимкнути*) |

- **Сонячні події** обчислюються за розташуванням із системних налаштувань ioBroker (широта й довгота) за допомогою [suncalc](https://github.com/mourner/suncalc). Без розташування такі таймери ігноруються з попередженням. У дні, коли події немає (полярні регіони), таймер не спрацьовує.
- Таймер надсилає ті самі команди, що й стани: у варіанті керування зонами *вибір зони* — до своєї зони (`light.zone` не змінюється), у варіанті *окремий канал для кожної зони* — через `zones.zone<n>` (усі зони: `light.*`). Шлюз підтверджує їх, як і будь-яку іншу команду.
- Якщо на момент спрацювання таймера шлюз не підключено, цей запуск пропускається (попередження в журналі) — пізніше він не повторюється.
- Таймери з неповними налаштуваннями ігноруються; причину вказують журнал і `timers.overview`.
- Час — це місцевий час системи ioBroker; перехід на літній час ураховується.
- **Таймери застосунку MiBoxer** зберігаються й виконуються в хмарі Tuya (вони лише вмикають або вимикають усі зони через точку даних 20 і потребують інтернету). Адаптер не може їх прочитати чи змінити, але бачить їхню дію в стані. Обидва види таймерів можна використовувати одночасно.

## Стани

| State | Опис |
| --- | --- |
| `info.connection` | Підключення до шлюзу |
| `info.ip` | Використовувана IP-адреса шлюзу |
| `light.on` | Увімк. / вимк. |
| `light.mode` | `white`, `colour` або `scene` — запис перемикає режим (кольоровий режим з останнім відтінком, режим сцени з останньою сценою) |
| `light.brightness` | Яскравість 1–100 % поточного режиму. 0 вимикає, значення більше 0 вмикає |
| `light.colorTemperature` | Колірна температура 2700–6500 K з кроком 100 K (перемикає в білий режим) |
| `light.color` | Колір у форматі `#rrggbb` за повної яскравості (перемикає в кольоровий режим). Запис задає відтінок і насиченість, яскравість значення RGB ігнорується — використовуйте `light.brightness` |
| `light.hue` | Відтінок 0–360° (перемикає в кольоровий режим) |
| `light.saturation` | Насиченість 0–100 % (перемикає в кольоровий режим) |
| `light.scene` | Сцена 1–9 (M1–M9 у застосунку), 0 = без сцени. Запис 1–9 запускає сцену |
| `light.speedUp` / `light.speedDown` | Кнопки S+ / S- застосунку: сцена швидше / повільніше. Шлюз не повідомляє швидкість |
| `light.countdown` | Секунди до перемикання ламп шлюзом (0 = вимк., стандартна точка даних 26) |
| `light.zone` | Лише під час вибору зони: зона команд `light.*`, 0 = усі зони, 1–8 |
| `zones.zone<n>.*` | Лише з окремим каналом для кожної зони: `on`, `mode`, `brightness`, `colorTemperature`, `color`, `hue`, `saturation`, `scene`, `speedUp`, `speedDown` для зони n |
| `dp101.raw` | Останній кадр точки даних 101 у Base64 — запис надсилає значення без змін |
| `dp101.hex` | Останній кадр точки даних 101 у шістнадцяткових байтах — запис надсилає кадр, контрольна сума додається або виправляється автоматично |
| `dp101.checksumValid` | Контрольна сума останнього кадру правильна |
| `dp101.history` | JSON-список останніх 50 кадрів (`rx` = отримано, `tx` = надіслано) з позначкою часу; повторювані однакові відповіді про стан не додаються |
| `raw.dp<n>` | Будь-яка інша точка даних, про яку повідомляє шлюз, створюється автоматично (доступна для запису) |
| `settings.dmxAddress` | Початкова адреса 1–512 входу DMX512 шлюзу — починаючи з неї шлюз використовує 5 каналів: червоний, зелений, синій, холодний білий, теплий білий (меню *DMX* у застосунку). Запис надсилає її до зони з `light.zone` (вибір зони) або до всіх зон; шлюз її підтверджує |
| `timers.active` | `false` призупиняє всі таймери (наприклад, на час відпустки або зі скрипта), `true` знову запускає їх |
| `timers.nextRun` | Наступний запуск таймера з назвою таймера (`paused (…)`, доки `timers.active` дорівнює `false`) |
| `timers.lastRun` | Останній запуск таймера з назвою та дією |
| `timers.overview` | JSON-список усіх таймерів: розклад, дія, наступний запуск, причина, якщо таймер ігнорується |

Значення, яким потрібен певний режим або увімкнені лампи, адаптер надсилає так само, як застосунок MiBoxer: наприклад, колірна температура в кольоровому режимі спочатку перемикає в білий режим, а яскравість за вимкнених ламп спочатку їх вмикає. Швидкі зміни (наприклад, з повзунка) об'єднуються, надсилається лише останнє значення. Команди приймаються лише за підключеного шлюзу. Команда вважається виконаною, коли наступний стан шлюзу показує її значення (приблизно через 2,5 с); до того стан не підтверджено.

## Точка даних 101 — протокол

WL-433 передає лампи, зони та сцени в специфічній для виробника точці даних 101: 12-байтові кадри в кодуванні Base64, останній байт — 8-бітна сума байтів 0–10. Формат розшифровано 22.09.2026 за кадрами стану реального шлюзу та командами, які застосунок MiBoxer записує у свій журнал Android:

| Кадр | Bytes (hex) | Значення |
| --- | --- | --- |
| Команда (застосунок / адаптер → шлюз) | `41 00 00 0B cc vv vv vv vv zz 80 ss` | `cc` команда: `01` відтінок 0–255 (значення в байтах 5–8, перемикає в кольоровий режим), `02` яскравість 1–100 %, `03` колірна температура 0–38 (2700 K + 100 K за крок), `04` насиченість 0–100 %, `05` сцена 1–9, `06` клавіша (`01` увімк., `02` вимк., `03` S-, `04` S+, `06` білий режим); `zz` зона: `00` усі, `01`–`08` |
| Запит стану | `43 00 00 80 00 00 00 00 00 80 80 C3` | шлюз відповідає кадром стану `44` |
| Стан (шлюз → застосунок) | `42` / `44` `00 00 00 mm hh tt bb ss 0B dd xx` | `42` повідомлення про зміну (приблизно через 2,5 с після останньої зміни), `44` відповідь на запит; `mm` режим: `00` вимк., `01` колір, `02` білий, `03`–`0B` сцена 1–9; `hh` відтінок, `tt` крок колірної температури, `bb` яскравість, `ss` насиченість (0 у білому режимі), `dd` молодший байт початкової адреси DMX. Зона не входить до стану |
| Початкова адреса DMX | `49 00 00 0B 02 aa aa 00 00 zz 80 ss` | `aa aa` адреса 1–512 (старший байт, молодший байт), `zz` зона; відповідь `49 00 00 0B 02 01 tt bb ss aa aa xx` |

Клавіша `06 05` теж вимикає лампи (повторне натискання залишає їх вимкненими, це не перемикач); чим вона відрізняється від `06 02`, поки невідомо, адаптер її не використовує. Стандартні точки даних 20–23 шлюз виводить із цих команд; адаптер використовує лише точку даних 20 (увімк./вимк., надходить раніше за стан), а в усьому іншому дотримується стану з точки даних 101. Запис точки даних кольору Tuya 24 не змінює колір ламп — застосунок MiBoxer її теж не використовує.

Прямий доступ для власних експериментів: `dp101.hex` приймає 11 байтів (контрольна сума додається), наприклад `43 00 00 80 00 00 00 00 00 80 80` запитує стан.

## Обмеження

- Шлюз повідомляє один стан для всіх ламп (останнє налаштування), а не стан кожної зони — див. [Зони](#зони).
- Швидкість сцени (S+ / S-) шлюз не повідомляє.
- Неможливо побачити, чи лампа справді отримала команду радіоканалом: стан надходить від шлюзу.
- Шлюз і далі повідомляє свій стан у хмару Tuya. Повне блокування доступу до інтернету може зробити його ненадійним. Таймерам застосунку MiBoxer потрібна хмара, таймерам адаптера — ні.

## Журналювання та пошук несправностей

Адаптер веде журнал за фіксованою схемою, щоб журнал у будь-який момент був корисним для пошуку помилок:

| Рівень | Що записується |
| --- | --- |
| error | Помилки конфігурації, через які адаптер не може працювати (немає ID пристрою, локальний ключ не з 16 символів) |
| warn | Проблеми, що потребують ваших дій, — повідомляються один раз, а потім лише на рівні debug, доки їх не усунуто: шлюз відхиляє підключення, дані неможливо розшифрувати (неправильний локальний ключ), команди не підтверджені шлюзом, запити стану без відповіді, неочікувані значення точок даних або кадри стану, таймери з неповними налаштуваннями або без розташування для сонячних подій, таймери, які не змогли перемкнути лампи, початкова адреса DMX, не підтверджена шлюзом |
| info | Ключові події: зведення конфігурації під час запуску, шлюз знайдено, підключено, підключення втрачено, підключення знову стабільне, об'єкти іншого варіанта зон видалено, кількість активних таймерів, таймери призупинено або знову активовано |
| debug | Кожен крок із вхідними даними, рішеннями та тривалістю: зміна стану → перетворення на кадри точки даних 101 (з причиною додаткових кадрів, наприклад «спочатку увімкнути») → черга команд → надсилання → підтвердження станом (або якого значення ще бракує), кожна отримана точка даних і стан та оновлені стани, запити стану, пошук, кожен таймер із розкладом, наступним запуском (сонячна подія, зсув, випадкове відхилення) і виконанням. Команди (`#12`) і спроби підключення (`Attempt #3`) пронумеровано, тож усі рядки однієї команди можна простежити |
| silly | Додатково трасування протоколу бібліотеки tuyapi (пакети, ping/pong) з міткою `[tuyapi]` |

Кожне повідомлення починається з мітки: `[cfg]` конфігурація, `[conn]` підключення, `[rx]` шлюз → стани, `[cmd]` стани → команди, `[queue]` черга команд, `[poll]` оновлення та запит стану, `[disc]` пошук, `[dp101]` необроблені кадри, `[timer]` таймери, `[unload]` завершення, `[tuyapi]` трасування бібліотеки. Локальний ключ і ключі сеансу ніколи не з'являються в журналі — зведення конфігурації показує лише довжину ключа.

Зміна рівня: Admin → **Екземпляри** → режим експерта → рівень журналу `miboxer-wl433.0` → `debug` (або `silly` для трасування протоколу; потім перезапустіть екземпляр). Додавайте журнал debug і вміст `dp101.history`, коли повідомляєте про проблему.

## Changelog
<!--
    Placeholder for the next version (at the beginning of the line):
    ### **WORK IN PROGRESS**
-->

### 0.2.0 (2026-09-22)

- (ssbingo) Локальні таймери на новій вкладці *Таймери* налаштувань екземпляра (до 50): час доби або сонячна подія зі зсувом і випадковим відхиленням, дні тижня, сезон, зона, будь-яка дія з освітленням і вимкнення через заданий час; стани `timers.active`, `timers.nextRun`, `timers.lastRun` і `timers.overview`
- (ssbingo) Початкову адресу входу DMX512 шлюзу можна читати й записувати (`settings.dmxAddress`)
- (ssbingo) Задокументовано: команду DMX, клавішу `06 05`, хмарні таймери застосунку MiBoxer; посібники з новим розділом про таймери

### 0.1.0 (2026-09-22)

- (ssbingo) Розшифровано точку даних 101: лампи, зони та сцени тепер керуються власними командами шлюзу (раніше колір не можна було задати), стан читається з точки даних 101 і запитується активно
- (ssbingo) Нові стани: відтінок, насиченість, сцена M1–M9, кнопки S+ / S-; зони можна вибрати в налаштуваннях як вибір зони (`light.zone`) або окремий канал для кожної зони
- (ssbingo) Команди підтверджуються станом шлюзу, якщо шлюз їх не підтверджує, записується попередження; докладний вивід debug для кожного кроку
- (ssbingo) Посібник користувача німецькою та англійською для початківців

### 0.0.1 (2026-09-21)

- (ssbingo) Перший випуск: локальне керування шлюзом WL-433 через протокол Tuya LAN (увімк./вимк., режим, яскравість, колірна температура, колір, зворотний відлік), прямий доступ до точки даних 101 з обробкою контрольної суми та пошук шлюзу в локальній мережі, детальне налагоджувальне журналювання з мітками компонентів, номерами команд і тривалістю (секрети ніколи не записуються)

## Ліцензія

Ліцензія MIT — Copyright (c) 2026 ssbingo. Повний текст ліцензії наведено в [English README](../../README.md#license) і у файлі [LICENSE](../../LICENSE).
